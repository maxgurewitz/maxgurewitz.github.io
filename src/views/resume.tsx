import * as t from '../types';
import * as React from 'react';

const resumeView : t.View = function mainView(payload) {
  const {state, dispatch} = payload;

  return (
    <div>
      resume content
    </div>
  );
}

export default resumeView;
